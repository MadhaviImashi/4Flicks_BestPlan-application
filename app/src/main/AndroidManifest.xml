<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a4flicksapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.4FlicksApp">
        <activity android:name=".update_recipe"></activity>
        <activity android:name=".create_recipe">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".all_recipes_with_recipes"></activity>
        <activity android:name=".all_recipes_without_any_recipes" />
        <activity android:name=".bmiCalculator" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>